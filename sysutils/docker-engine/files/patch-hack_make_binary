--- hack/make/binary.orig	2021-05-05 22:14:59 UTC
+++ hack/make/binary
@@ -4,7 +4,7 @@ rm -rf "$DEST"
 
 # This script exists as backwards compatibility for CI
 (
-	DEST="${DEST}-daemon"
-	ABS_DEST="${ABS_DEST}-daemon"
+	DEST="${DEST}"
+	ABS_DEST="${ABS_DEST}"
 	. hack/make/binary-daemon
 )
